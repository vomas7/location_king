# Зависимости для разработки и CI. Прод-зависимости — в requirements.txt
-r requirements.txt

# Линтер и форматтер
ruff==0.9.4

# Тесты. Pillow здесь больше не нужен: он переехал в прод-зависимости, когда
# аватарку стало можно загружать, и приезжает сюда вместе с requirements.txt
pytest==8.1.1
pytest-asyncio==0.23.5
pytest-cov==5.0.0
