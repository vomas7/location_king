"""
Схемы знакомства с игрой без учётной записи.

Их всего одна: раунды и результаты знакомство отдаёт теми же схемами, что и
настоящая игра. Так экран игры разбирает их, не зная, что перед ним гость, —
а второй набор схем однажды разошёлся бы с первым.
"""

from pydantic import BaseModel

from app.schemas.game import RoundView


class DemoRoundsResponse(BaseModel):
    """
    Все раунды знакомства сразу.

    Целиком, а не по одному: раундов пять, они одни и те же у всех, и просить
    сервер о каждом отдельно значило бы держать паузу перед каждым снимком
    там, где её может не быть вовсе.
    """

    rounds: list[RoundView]
