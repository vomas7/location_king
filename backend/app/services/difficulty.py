"""
Что означает уровень, кроме набора мест.

Раньше ширину кадра игрок выбирал сам, отдельным переключателем рядом с
уровнем. Это был самый непонятный элемент меню: «сорок километров» ничего не
говорит о том, будет ли трудно, а два независимых регулятора сложности легко
свести в бессмысленную пару — дикая природа в кадре на пять километров не
угадывается вообще никак.

Теперь кадр — свойство уровня, и он не усиливает сложность, а делает место
читаемым: чем больше зона, тем больше нужно контекста, чтобы в кадр попал не
безымянный квартал, а излучина реки, берег или хребет, по которым место и
узнают.

Числа выбраны по статистике первых полутысячи раундов. Кадр до двадцати
километров давал средний промах в 4170 километров, от сорока пяти до
девяноста — 2895: узкий кадр показывал сетку кварталов, по которой нельзя
опознать даже Шанхай. Поэтому кадр начинается с сорока пяти километров —
столько занимает город из каталога вместе с берегом и рекой — и растёт вслед
за размером зоны: на «сложно» зоны вчетверо больше городских, на «хардкоре» —
в пятнадцать раз.

«Легко» и «средне» получают один и тот же кадр: зоны у них одного размера, а
разница между уровнями — в том, какие это города, а не в том, сколько видно.

Числа стоит пересматривать по той же статистике — `scripts/stats.py --only
extent` и `--only scoring`.

Это заказ, а не точное значение: участок раунда — один тайл Web Mercator, и
его настоящая ширина зависит от широты. Кадр раунда получается ближайшим к
заказанному, и в базе лежит именно он.
"""

from app.models.enums import Difficulty

#: Сколько земли попадает в кадр на каждом уровне, в километрах
VIEW_EXTENT_KM: dict[str, float] = {
    Difficulty.EASY: 45.0,
    Difficulty.NORMAL: 45.0,
    Difficulty.HARD: 60.0,
    Difficulty.HARDCORE: 100.0,
}

#: Кадр партии без указанного уровня. Столько же, сколько у «средне»: это и
#: есть уровень по умолчанию
DEFAULT_VIEW_EXTENT_KM = VIEW_EXTENT_KM[Difficulty.NORMAL]


def view_extent_km(difficulty: str | None) -> float:
    """Ширина кадра для уровня. Неизвестный уровень получает кадр «средне»."""
    if difficulty is None:
        return DEFAULT_VIEW_EXTENT_KM

    return VIEW_EXTENT_KM.get(difficulty, DEFAULT_VIEW_EXTENT_KM)
