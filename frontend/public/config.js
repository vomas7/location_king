// Конфигурация клиента. Файл читается браузером как есть и может быть заменён
// при деплое, не пересобирая приложение.
//
// apiBase пустой — API живёт на том же origin, что и страница. Так не бывает
// mixed content на https и не нужен CORS.
window.__CONFIG__ = {
  apiBase: "",
};
