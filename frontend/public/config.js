// Конфигурация клиента. Файл читается браузером как есть, поэтому его можно
// заменить, не трогая остальную сборку.
//
// Эта версия — значения по умолчанию для разработки. В прод-образе файл
// собирается заново скриптом scripts/make-config.mjs из переменных .env
// (OPERATOR_NAME, OPERATOR_EMAIL), чтобы реквизиты не лежали в репозитории.
//
// apiBase пустой — API живёт на том же origin, что и страница. Так не бывает
// mixed content на https и не нужен CORS.
//
// operator — кто отвечает за сервис. Эти значения показываются в условиях
// использования и политике конфиденциальности: без них документы бесполезны,
// потому что непонятно, кому писать.
window.__CONFIG__ = {
  apiBase: "",
  operator: {
    name: "",
    email: "",
  },
};
