// Конфигурация клиента. Подставляется при деплое: nginx или CI могут
// переписать этот файл, не пересобирая приложение.
//
// apiBase пустой — значит, API живёт на том же origin, что и страница,
// и запросы идут на /api/... Так не бывает mixed content на https.
window.__CONFIG__ = {
    apiBase: "",
};
